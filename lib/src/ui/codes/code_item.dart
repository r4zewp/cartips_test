import 'package:flutter/material.dart';

class CodeItem {
  const CodeItem({required this.name, required this.color});

  /// QR appears to be single for each item
  /// As it is mock data.
  final String name;
  final Color color;
}
